---
import Layout from '../../layouts/Layout.astro';
import { NEXT_PAGES } from '../../data/site-nav';
---

<Layout
	title="( Belong )"
	breadcrumbItems={[{ label: 'Belong', href: null }]}
	nextPage={NEXT_PAGES.belong}
/>
